<!DOCTYPE html>
<html lang="en">
  <head>
    <title>EJS Example</title>
  </head>
  <body>
    <h1>Even Odd</h1>

    <% let x = Math.floor((Math.random() * 10) + 1) %>
    <p>The number is <%=x%></p>
    <br />
    <% if (x % 2 === 0) { %>
    <p>Number is even</p>
    <% } else { %>
    <p>Number is odd</p>
    <%}%>
  </body>
</html>
