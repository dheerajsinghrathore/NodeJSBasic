// Here we are exporting two functions: add and subtract
// function defined and assigned to exports object

exports.add = (x, y) => {
  return x + y;
};
exports.subtract = (x, y) => {
  return x - y;
}
